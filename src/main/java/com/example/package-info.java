/**
 * JavaFX basketball game package for Hoops Showdown.
 *
 * <p>The package is organized around three main classes:</p>
 *
 * <ul>
 *   <li>{@link com.example.FinalProj} builds the JavaFX screens, animations,
 *       controls, and visual effects.</li>
 *   <li>{@link com.example.Data} stores match state, possession, and shot
 *       probability logic.</li>
 *   <li>{@link com.example.Player} stores each player's score, selected
 *       character, position, and shot-zone logic.</li>
 * </ul>
 *
 * <p>The game uses a shared keyboard for two players. Player 1 moves with
 * {@code W/A/S/D} and shoots or blocks with {@code E}. Player 2 moves with
 * {@code I/J/K/L} and shoots or blocks with {@code O}. The first player to 11
 * points wins.</p>
 */
package com.example;
